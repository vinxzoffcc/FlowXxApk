<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/bg">
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:padding="16dp">

        <TextView android:text="Setelan" android:textSize="24sp" android:textStyle="bold"
            android:textColor="@color/text_primary" android:layout_marginTop="12dp" android:layout_marginBottom="16dp"/>

        <!-- Dev card -->
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical" android:background="@drawable/bg_card"
            android:padding="20dp" android:gravity="center" android:layout_marginBottom="16dp">
            <TextView android:text="FlowX" android:textSize="28sp" android:textStyle="bold"
                android:textColor="@color/accent" android:layout_marginBottom="4dp"/>
            <TextView android:text="FlowX Game Optimizer" android:textSize="13sp" android:textStyle="bold" android:textColor="@color/text_primary"/>
            <TextView android:text="Dibuat oleh Vinz" android:textSize="11sp" android:textColor="@color/text_hint"/>
            <TextView android:text="v1.0 · Non-Root · Shizuku" android:textSize="10sp"
                android:textColor="@color/text_hint" android:layout_marginTop="8dp"
                android:background="@drawable/bg_pill" android:paddingHorizontal="12dp" android:paddingVertical="4dp"/>
        </LinearLayout>

        <!-- API Key -->
        <TextView android:text="API KEY GEMINI" android:textSize="11sp" android:textAllCaps="true"
            android:textColor="@color/text_hint" android:layout_marginBottom="8dp"/>

        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical" android:background="@drawable/bg_card"
            android:padding="14dp" android:layout_marginBottom="12dp">

            <TextView android:text="Gemini API Key" android:textSize="13sp" android:textStyle="bold"
                android:layout_marginBottom="4dp"/>
            <TextView android:textSize="11sp" android:textColor="@color/text_hint"
                android:text="Ambil gratis di aistudio.google.com → Get API Key"
                android:layout_marginBottom="10dp"/>

            <TextView android:id="@+id/tv_key_status"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:text="API key belum diset" android:textSize="12sp"
                android:textColor="@color/text_secondary" android:layout_marginBottom="10dp"/>

            <EditText android:id="@+id/et_api_key"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:hint="AIza..." android:inputType="textPassword"
                android:background="@drawable/bg_input" android:padding="10dp"
                android:textSize="13sp" android:layout_marginBottom="10dp"/>

            <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
                android:orientation="horizontal" android:gravity="end">
                <Button android:id="@+id/btn_clear_key"
                    android:layout_width="wrap_content" android:layout_height="40dp"
                    android:text="Hapus" android:textSize="12sp"
                    android:backgroundTint="@color/border" android:textColor="@color/text_secondary"
                    android:layout_marginEnd="8dp"/>
                <Button android:id="@+id/btn_save_key"
                    android:layout_width="wrap_content" android:layout_height="40dp"
                    android:text="Simpan" android:textSize="12sp"
                    android:backgroundTint="@color/accent"/>
            </LinearLayout>
        </LinearLayout>

    </LinearLayout>
</ScrollView>
